Project Documentation


О проекте

Этот проект содержит автоматизированные тесты для веб-приложений с использованием:

Selenium WebDriver для автоматизации браузера

Pytest как фреймворк для тестирования

Allure для создания детальных отчетов

Page Object Pattern для структурирования кода



 Структура проекта

10_lesson/
├── FormPage/                          # Тесты формы валидации
│   ├── BasePage.py                   # Базовый класс страницы
│   ├── formpage.py                   # Page Object для формы
│   ├── test_form.py                  # Тестовые сценарии формы
│   ├── __init__.py                   # Инициализатор пакета
│   └── allure-results/               # Результаты Allure (генерируется)
│
├── ShopPage/                         # Тесты интернет-магазина SauceDemo
│   ├── BasePage.py                   # Базовый класс страницы
│   ├── LoginPage.py                  # Page Object для страницы логина
│   ├── InventoryPage.py              # Page Object для страницы товаров
│   ├── CartPage.py                   # Page Object для корзины
│   ├── CheckoutPage.py               # Page Object для оформления заказа
│   ├── test_03_shoppage_obj.py       # Тестовые сценарии магазина
│   ├── __init__.py                   # Инициализатор пакета
│   └── allure-results/               # Результаты Allure (генерируется)
│
└── README.md                         # Документация проекта


Установка зависимостей

Требуемые пакеты Python:

bash
pip install -r requirements.txt


requirements.txt:

txt

selenium>=4.15.0
pytest>=7.4.0
allure-pytest>=2.13.0
pytest-html>=4.1.0

Установка Allure Framework:
Windows (рекомендуется):
powershell
# Скачать и установить вручную
Invoke-WebRequest -Uri "https://github.com/allure-framework/allure2/releases/download/2.27.0/allure-2.27.0.zip" -OutFile "C:\allure\allure.zip"
Expand-Archive -Path "C:\allure\allure.zip" -DestinationPath "C:\allure\" -Force
$env:Path += ";C:\allure\allure-2.27.0\bin"
Проверка установки:
bash
allure --version


🚀 Запуск тестов
Тесты формы валидации:
bash
cd FormPage

# Базовый запуск
pytest test_form.py -v

# С генерацией Allure отчетов
pytest test_form.py --alluredir=allure-results -v

# С HTML отчетом
pytest test_form.py --html=report.html --self-contained-html -v


Тесты интернет-магазина:
bash
cd ShopPage

# Базовый запуск
pytest test_03_shoppage_obj.py -v

# С генерацией Allure отчетов
pytest test_03_shoppage_obj.py --alluredir=allure-results -v



# Запуск конкретного теста
pytest test_03_shoppage_obj.py::TestSauceDemo::test_complete_purchase_flow -v
Все тесты в директории:
bash
pytest --alluredir=allure-results -v
📊 Просмотр отчетов
Allure отчет (рекомендуется):
bash
# Запуск сервера с отчетом
allure serve allure-results


# Генерация статического отчета
allure generate allure-results -o allure-report --clean
allure open allure-report
HTML отчет (альтернатива):
bash
pytest --html=report.html --self-contained-html -v
# Открыть report.html в браузере


🧪 Описание тестовых сценариев
Форма валидации (FormPage/)
test_form_validation_empty_zip - проверка валидации с пустым ZIP-кодом

test_form_validation_valid_zip - проверка с валидным ZIP-кодом

test_page_opens - базовая проверка загрузки страницы



Интернет-магазин (ShopPage/)
test_complete_purchase_flow - полный цикл покупки товаров

test_invalid_login - проверка авторизации с неверными данными

test_add_different_products - проверка добавления различных товаров

🛠️ Классы и методы

BasePage
Базовый класс с общими методами для всех страниц:

find_element(by, value) - поиск элемента с ожиданием

find_clickable_element(by, value) - поиск кликабельного элемента



FormPage
Методы для работы с формой:

fill_all_fields(form_data) - заполнение всех полей формы

submit_form() - отправка формы

verify_zip_code_error() - проверка ошибки ZIP-кода

verify_valid_fields_success() - проверка успешной валидации



LoginPage
Методы для авторизации:

login_as_standard_user() - авторизация под стандартным пользователем

enter_username(username), enter_password(password) - ввод данных




InventoryPage
Методы для работы с товарами:

add_product_to_cart(product_name) - добавление товара в корзину

get_cart_items_count() - получение количества товаров в корзине

go_to_cart() - переход в корзину



CartPage
Методы для работы с корзиной:

click_checkout() - переход к оформлению заказа

get_cart_items_count() - подсчет товаров в корзине



CheckoutPage
Методы для оформления заказа:

fill_checkout_info(first_name, last_name, postal_code) - заполнение данных

get_total_amount() - получение итоговой суммы



🔧 Настройка окружения

Переменные окружения (опционально):
bash
# Браузер для тестов (по умолчанию Chrome)
set BROWSER=chrome



# Таймауты ожидания

set IMPLICIT_WAIT=10
set EXPLICIT_WAIT=20



Поддерживаемые браузеры:

Chrome (по умолчанию)
Firefox
Edge



